{
  "name": "api-config-detector",
  "version": "1.0.0",
  "description": "OpenClaw API Config Detector",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "echo 'Static site with Edge Functions'"
  },
  "engines": {
    "node": "18.x"
  }
}
